'use client'
import { styled, Box } from "@mui/material"

export const HomePageBox = styled(Box)(({theme}) =>({
    display: 'flex',
    flexDirection: 'row',
    width: '100vw',
    height: '100vh'
   
}))